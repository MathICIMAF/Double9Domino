<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.amg.double9domino">

    <uses-permission android:name="android.permission.VIBRATE" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:allowTaskReparenting="false"
        android:theme="@style/AppTheme" >

    <activity android:exported="true" android:theme="@style/Theme.AppCompat.Light.DarkActionBar" android:label="@string/title_activity_game" android:name="com.amg.double9domino.GameActivity" android:screenOrientation="fullSensor" android:configChanges="locale|keyboard|keyboardHidden|orientation|screenSize" >
        <intent-filter>
            <action android:name="android.intent.action.MAIN"/>
            <category android:name="android.intent.category.LAUNCHER"/>
        </intent-filter>
    </activity>
        <activity android:name=".MySettingsActivity"
            android:theme="@style/Theme.AppCompat.Light.DarkActionBar"
            android:parentActivityName=".GameActivity">

        </activity>
    </application>
</manifest>